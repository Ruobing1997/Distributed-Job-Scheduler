apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc1
  namespace: supernova
spec:
  storageClassName: "local-storage"
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  