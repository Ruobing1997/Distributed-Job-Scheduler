apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: grafana-pvc0
  namespace: supernova
spec:
  storageClassName: local-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi